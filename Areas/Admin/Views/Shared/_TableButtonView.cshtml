@model MemberShipWebsite.Areas.Models.SmallButtonModel

@using MemberShipWebsite.Areas.Models


<td style="width:400px;">
    <div class="btn btn-group" role="group">
   @Html.Partial("_SmallButtonView", new SmallButtonModel
   {
       Text = "Edit",
       Action = "Edit",
       icon = "edit",
       ButtonType = "btn-primary",
       Id = Model.Id,
       ItemId = Model.ItemId,
       ProductId = Model.ProductId,
       SubscriptionId = Model.SubscriptionId

   }) 

        @Html.Partial("_SmallButtonView", new SmallButtonModel
    {
        Text = "Details",
        Action = "Details",
        icon = "info-circle",
        ButtonType = "btn-warning",
        Id = Model.Id,
        ItemId = Model.ItemId,
        ProductId = Model.ProductId,
        SubscriptionId = Model.SubscriptionId

    })
       

   @Html.Partial("_SmallButtonView", new SmallButtonModel
   {
       Text = "Delete",
       Action = "Delete",
       icon = "trash-o",
       ButtonType = "btn-danger",
       Id = Model.Id,
       ItemId = Model.ItemId,
       ProductId = Model.ProductId,
       SubscriptionId = Model.SubscriptionId

   })
    </div>
</td>

